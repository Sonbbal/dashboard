import React from 'react';
import AppBar from '@material-ui/core/AppBar';

const Header = () => {
    return (
        <div>
            <AppBar>
                <div>로고 들어가는곳</div>
            </AppBar>
        </div>

    )
}

export default Header;